FrankenText

Generates random sentences by analysing the text of Frankenstein (pg84.txt). The program reads the book, extracts up to 50 unique words as tokens, and maps which words tend to follow each other. It then generates a new sentence by randomly picking successors. Compile and run main.c from the src/ folder.